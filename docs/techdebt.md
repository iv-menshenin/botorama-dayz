# Техдолг botorama

Живой список накопленного долга: мёртвый код, заглушки, отложенные фичи и
архитектурные хвосты. Пополняется/чистится по мере работы. Отложенные
функциональные фичи (pathfinding и его подзадачи) детально — в
`fsm-implementation-plan.md`; здесь — только ссылки, без дублирования.

Приоритет: **высокий / средний / низкий** (по ощущению «когда начнёт мешать»).

---

## A. Мёртвый / неиспользуемый код

- **средний** — `dmAISurvivor.FacePoint()` (`core/4_World/Entities/Bot/dmAISurvivor.c:347`) —
  после реворка `MoveTo` никто не зовёт. Оставили «на всякий» как примитив; решить —
  удалить или задокументировать назначение.
- **низкий** — `dmBotIntent_LookAround` (`Intent/dmBotIntent_LookAround.c`) — реализован,
  но нигде не инстанцируется (Idle использует `Glance`/`Turn`). Задействовать или удалить.
- **средний** — `dmBotPreset_Hunter` + скелет-состояния (`Presets/dmBotPreset_Hunter.c`,
  `States/dmBotState_{Hunting,Fighting,Surrender}.c`) — демо-фабрика не
  подключена; реальный FSM строится через `/fsm` builder. Папка `Presets/` по сути
  мёртвая и вводит в заблуждение (см. D-13). Внимание: пресет ссылается на `Stealth`
  со старым смыслом «low health» — теперь `Stealth` про укрытие (см. план).
- **средний** — `dmTarget` + `AddTarget/ClearTargets/GetTargets` (`dmTarget.c`,
  `dmAISurvivor.c`) — скелет «памяти целей», в поведении не используется.
  Ожидает Phase 4+ (скан признаков/бой).

## B. Заглушки (stubs)

- **средний** — `dmAISurvivor.HasPlayerSigns()` — всегда `false` (TODO: скан признаков
  игрока — лут/следы).
- **средний** — скелет-состояния `Hunting/Fighting/Surrender` — `OnUpdate` сразу
  `EXIT`, поведения нет (`Stealth` уже реализован — укрытие).
- **низкий** — условие `dmBotCondition_HasPlayerSigns` — опирается на заглушку
  `HasPlayerSigns()` выше (`NoAmmo` уже реальная инспекция магазина).

## C. Отложенные фичи (детали — в `fsm-implementation-plan.md`)

- **средний** — Phase 3: параметры FSM из JSON. Модуль `dmJsonFile`/`dmJsonConfigBase`
  готов, но к FSM не привязан (`fsm_<preset>.json` не читается).
- **средний** — Phase 5: pathfinding — доводка. Сделано: recovery при зависании, двери,
  vault/climb, лестницы (задачи 1, 7–9). Осталось: плавание, attachment-navmesh,
  string-pulling, троттлинг пересчёта, кост-тюнинг. Секция «Pathfinding» в плане.
- **низкий** — критерий Phase 1: тест распределения весов (лог частот переходов ≈ весам)
  ни разу не прогонялся.
- **средний** — Зрение: уйти от scan-box (физики). Сейчас `dmVision` берёт всех в боксе
  `DM_PERCEPTION_RADIUS=30 м` (`Scene/PhysicsGetEntitiesInBox`) — это не масштабируется
  на дальний детект. Цель: зомби/животные — все в радиусе 50 м из глобального регистра;
  игроки — сканировать всех в радиусе 1 км. Если ванильного глобального регистра для
  зомби/животных нет — закастомить: регистрировать в конструкторе, снимать в деструкторе.
- **средний** — Опасная высота / fall-safety: полноценной проверки нет. Во фланге
  (`docs/plans/combat-movement.md`) используется упрощённый гард через `g_Game.SurfaceY(x, z)`
  (разница высоты path-точки и поверхности ≤ 1.5 м), но это не защищает от смертельного
  падения. Эталон — Expansion `eAI_IsDangerousAltitude` (`fallHeight = position[1] - surfaceY
  >= DayZPlayerImplementFallDamage.HEALTH_HEIGHT_LOW`, с учётом глубины воды). Нужно в
  навигации и в боевом движении.

## D. Архитектурный долг

- **средний** — `LoadFSM(presetName)` не реализован — в плане был метод загрузки пресета
  по имени; в коде есть только `SetFSM` + билдер `/fsm`. Пресеты по имени не грузятся
  (связано с A: `Presets/` мёртв).
- **средний** — `Despawn` не чистит ссылки (`dmAISurvivor.Despawn`) — удаляет пешку, но
  не сбрасывает `m_FSM`, пулы интентов, `m_Targets`. Ref-циклы/утечки (в `codeguide.md`
  уже отмечено «чистить явно» — не сделано).
- **низкий** — пер-канальный арбитраж («CRITICAL+PARALLEL переопределяет движение
  CRITICAL+EXCLUSIVE») — отложен; сейчас exclusive глушит всё безусловно.
- **сделано** — **кастомный movement-command** (правильный доворот на ходу). Все три фазы
  закрыты (`7596ade`, `309fd3b`, `5043847`): `HeadingModel` override → консолидация применения
  в пешке → рамп скорости + доворот-с-притормаживанием + политика комфорта корпуса.
- **низкий (отложено до Fight)** — **непрерывная «наименьшая дискомфортность»**: текущая
  политика `ComputeBodyYaw` дискретна (`FULL`/`AUTO`/движение). Непрерывный компромисс
  (`w_head·|headAngle| + w_move·|moveAngle|`) нужен, когда появится одновременное «двигаться
  к укрытию + смотреть на врага» (Fight/стелс). Каркас (`m_MoveYaw` + `ComputeBodyYaw`) уже есть.

## E. Гигиена / процесс

- Рабочее дерево чистое (последний коммит `5043847`); дальше инкрементировать версию
  (`DM_BOTORAMA_VERSION`) по соглашению на каждое изменение.
- **E2E-мост (v3.167)** — два gap'а фазы 3:
  - `spawn` не применяет FSM-пресет (в отличие от `/bot spawn test`) → `snapshot.State`
    пуст, а условие `state` бесполезно, пока не задан пресет (только `follow` его ставит).
    Рассмотреть: поле `Preset` у `spawn` или отдельный op `preset`.
  - `clearall`/`killall` дёргают ГЛОБАЛЬНЫЙ `dmAISurvivor.ClearAll()` → чистят и фоновых
    settlement-ботов (spawn-менеджер), а не только named-бота теста. Тест не изолирован.
    Рассмотреть: `clearbot`/`killbot` по имени.
  - **Находки `dayz-reviewer` (фаза 3, коммиты `96c060a`/`9289be0`)**:
    - *(важно)* дублирование guard'а «no such bot» ×9 → вынести `FindBot(step, out bot, r)`.
    - *(важно)* асимметрия очистки: боты намеренно переживают job (для наблюдателя),
      но `clearall` обязателен как финальный шаг сценария (комментарий `ClearNamed`
      исправлен, v3.168).
    - *(косметика)* `Who` (spawnobj) vs `Obj` (getpos/setpos/clearobj) — два имени одного
      ключа; `ResolveWorldPos` sentinel `Y==0`; `EnsureDir` дублирует `dmJsonFile`; детект
      `moving` продублирован (`RunSnapshot`/`EvaluateCondition`); `EvaluateCondition`
      двойной сигнал (bool + `r.Ok`); null-защита `hit.component`; магическая `5` в `BaseName`.
  - **Находки `dayz-reviewer` (perf-ops, `7207aee`/`678e04e`)**:
    - *(косметика)* `dmE2EBridge.GiveWeapon` дословно копирует `dmLaunchCommand.GiveWeapon`
      — вынести общий хелпер `dmTestLoadout.GiveRandomRifle(PlayerBase)` в `test/4_World`
      (5_Mission может звать 4_World, обратно — нет).
    - *(косметика)* `RollArmySpawn` повторно снапает уже заснапанный `center` (двойной
      ground-snap; `center[1]` намеренно отбрасывается — задокументировать или убрать).

## F. Движение / качество ходьбы (замечания из тестов)

- **средний** — `MoveTo`: ступни «проскальзывают» вправо при ходьбе, бот идёт чуть
  правым плечом, а не ровно. Похоже на артефакт движения относительно корпуса
  (`OverrideMovementAngle`) + слайд-поворота — надо выровнять.
- **средний** — пауза между точками патруля: `MoveTo` завершается сразу (паузы в нём
  нет), задержку даёт `Patrol` (`DM_PATROL_DWELL_TIME = 5.0`). Хотим, чтобы патруль
  шёл без задержек → убрать/сократить dwell (решить, оставлять ли короткую остановку).
- **низкий (далёкое будущее)** — расширение recovery: когда научим персонажа
  пригибаться — при зависании пробовать пригнуться; потом прыгать; и только перепробовав
  всё (бок/пригибание/прыжок) — считать зависшим и `Fail()`.
- **средний** — Мили: бот иногда стоит в неудобной позиции и не выравнивается под бой
  (доворот/позиционирование при подходе).
- **средний** — Мили: звук/искры удара «по твёрдому» — `hitZoneIdx = -1` (нет component
  index без райкаста); фикс — получить index через коллизию/melee-targeting или кастомный звук.
- **низкий** — Двери: hand-анимация открывания добавлена (`dmBotIntent_OpenDoor` →
  `AddCommandModifier_Action(CMD_ACTIONMOD_OPENDOORFW)`, research в `navigation.md`).
  Проверить визуально, что жест реально играется (граф уже содержит `OpenDoorErc/Cro`);
  при предмете в руках возможен ре-синк рук → `RequestHandAnimationStateRefresh()`.
  Гейт против full-body команд и таймаут+`DeleteCommandModifier_Action` — уже в коде.

## G. Интенты: жизненный цикл (гигиена пула)

- **автодедлайн (сделано, `2.9`)**: интент без дедлайна (`m_Deadline < 0`), проживший в
  пуле дольше `DM_INTENT_MAX_AGE` (5 мин), удаляется арбитражем. Ни один интент не живёт
  вечно.
- **правило для State**: состояние обязано следить, что его интенты ещё действуют, и
  **пересоздавать** их, если их перезатёрли/удалили (например автодедлайном). Сейчас
  `Stealth` держит stance-интент с `deadline = -1` — после 5 мин он умрёт, и состояние
  должно его пересоздать (пока не реализовано).
- `/bot intent`-приказы (командный пул) получили дедлайн 1 мин (`goto`, `stance`), чтобы
  не копиться; `crouch` — 5 мин, `lookAt`/`lookAtMe` — 30 с.

---

## G2. Мили (ближний бой)

- **средний** — **Остаточный спин при ударе (~267°, редкий 1/288 ударов)**. После фикса
  (лок `HeadingModel` для MELEE2 + пропуск `ApplyBodyTurn` во время удара) спин снизился
  308.6°→267.7° и перестал вызывать stall, но не исчез. По эмпирике остаточный доворот —
  от самой MELEE2-анимации (root motion поворота к цели), а не от нашего кода. Дожать:
  разобрать ванильный `StartCommand_Melee2`/анимацию качелей (крутит ли корпус) и, если
  можно, заблокировать доворот.
- **низкий** — **Порог дистанции удара**: `HitTo` считает `dist = длина - 0.65` и режет по
  `m_ReachDistance`; при мачете бот часто на 0.65–0.81 м → ~40 ложных «дистанция»-отказов
  за бой. Пересмотреть порог/вычисление достижимости (2D + высота уже учтены, но константа
  0.65 грубовата).
- **низкий (TODO)** — **«Человекоподобное поведение» при угрозе**: сейчас развязанный бот
  при враждебной цели всегда убегает (`EscapeDanger`). Позже: если бота держат под прицелом
  (явная угроза оружием) — НЕ убегать; разделить «убегать» vs «под прицелом». Связано с
  развязыванием (`dmBotIntent_Untie` + `EscapeDanger`).

## H. Прицеливание (огнестрел, dmAiming)

- **высокий** — **Визуальный ствол не следует за прицелом** (следствие готчи из `combat.md`):
  фактическая ось ствола (`GetSelectionPositionMS("usti/konec hlavne")` → `GetBarrelDirection()`)
  гонится движковой aim-моделью и НЕ совпадает со скриптовым `m_AimRelAngleLR/UD`. Поэтому
  направление выстрела сейчас = скриптовый прицел (`m_AimWorldDirection`), а ствол визуально
  «вниз». Задача: довести граф анимаций (`Locomotion.agr` `AnimNodePose2`/`AnimNodeWeaponIK`
  читать `dmAI_AimX/Y` вместо `AimX/Y`) до состояния, когда memory-points реально крутятся за
  нашим прицелом; затем — вернуть стрельбу по `GetBarrelDirection()` (убрать рассинхрон дуло↔прицел).
  Рецепт — `docs/research/combat.md` «Минимальный рецепт для botorama».
- **сделано** — **Отдача на выстрел** (recoil): `dmBot_Fire` → `AddRecoil(DM_AIM_RECOIL_MODIFIER)`,
  `dmAiming.OnUpdate` гасит `m_RecoilPitch` и прибавляет к питчу (обе ветки). Осталось:
  реальная формула `recoilModifier` от веса/патрона.
- **средний** — **Strafe-factor пропущен**: `eAIAimingProfile` штрафует точность за
  «хаотичный стрейф» цели через `DayZPlayerImplement.GetStrafeFactor()` — это кастомный
  метод из dayz-devaliada, в ванили его нет. В `dmAiming` блок strafe НЕ перенесён
  (`strafeFactor = 0`). Ввести свой аналог или отказаться.
- **средний** — **Видимость — заглушка 100%**: `eAI_GetVisibility` в `dmAiming` заменена
  константой `1.0` (ночь/туман/дым не снижают точность). Ввести свою модель видимости.
- **сделано** — **Выстрел без round-trip**: `dmAiming.OnUpdate` кладёт в пешку сразу мировое
  направление через `SetAim(...)`; `ComputeShot` берёт `GetAimWorldDirection()` напрямую
  (раньше — пересборка из `m_AimRelAngleLR/UD` через `GetWeaponAimDirection()`, а интент
  писал углы через `SetAimDirection` → малый рассинхрон при довороте корпуса). Визуальный
  ствол по-прежнему на сглаженных углах.
- **низкий** — **Самопроверка разброса**: `/test bot aim` теперь гоняет новый путь
  (Shooting + `dmBotIntent_Aim` + `dmAiming` + перезарядка), но явной самопроверки
  разброса (выстрелы-до-убийства как PASS/FAIL) нет — сейчас это наблюдение.
- **сделано** — **Ванильный `DropBullet`-шум** (`WeaponChambering_Base.DropBullet` кидал
  `Error("cannot drop Bullet - lost")` при аборте посреди доствольного цикла — на смерти
  `OnCommandDeathStart` и перелезании/прыжке `OnCommandClimbStart`). Фикс: переопределён
  `DropBullet` в `reg/4_World/modded_WeaponChambering.c` — при провале `HandleDropCartridge`
  просто `return false` без `Error()`. (Раньше это шло через `OnCommandMelee2Start` при
  переходе в ближний бой — тот путь ушёл вместе с абортом v3.37.)
- **сделано** — **Десинк FSM `DoubleBarrel_Base` после дабл-выстрела (B95, режим `Double`)**:
  после выстрела из обоих стволов физически патронник «стреляный» (`IsChamberFiredOut=true`),
  но FSM-состояние застревало в `DoubleBarrelLoadedLoaded` (не переходило в `FireoutFireout`),
  из-за чего событие перезарядки `LOAD1_BULLET` отклонялось (`failed to perform weaponevent ...
  in state DoubleBarrelLoadedLoaded ...`). Корень: натив `Fire(mi,pos,dir,speed)` (явное направление,
  `dmBot_Fire`) НЕ помечает ствол «стреляным» (в отличие от `TryFireWeapon`), а натива
  `SetChamberFiredOut` нет. Фикс: после `Fire()` в `WeaponFireMultiMuzzle.OnEntry` вызываются
  `EjectCartridge(mi, dmg, type)` (полный → пустой) + `EffectBulletHide(mi)`, затем
  `RandomizeFSMState()` ресинкает FSM в `EmptyEmpty` → `LOAD1_BULLET` принимается. Это наш кейс
  парных стволов; Expansion эту проблему не решает (детали — `docs/research/combat.md`).

## I. Баги из `bugreport.txt` (свежие замечания тестов)

Свежие баги из тестов (пронумерованы как в `bugreport.txt`). Каждый — с гипотезой
корня. Статус `[~]` = гипотеза сформулирована, фикс не сделан (по запросу — только анализ).

1. `[~]` **Зараженная зона → телепорт**. Бот заходит в газ-зону, ваниль кидает
   «Персонаж перемещён из опасной зоны» и телепортирует бота. *Сделано (v3.46)*: override
   `OnContaminatedAreaEnterServer()` no-op в пешке (убирает телепорт и газ). **Осталось (TODO)**:
   правильное избегание газ-зон в навигации — порт Expansion `s_Expansion_DangerousAreas` +
   `FindClosestPointOutsideCluster` (детали — `docs/research/navigation.md`).

2. `[~]` **Костёр: наступает + бесконечно атакует.** (а) наступает в костёр (нет избегания
   опасных мест); (б) после этого атакует костёр бесконечно. *Гипотеза*: (а) нет гарда
   опасных мест вокруг конечной точки маршрута; (б) та же причина, что #3 (неживой объект
   попал в цели). Связано с #3.

3. `[~]` **Атакует бесполезную `EntityAI` (автомобиль, костёр).** *Гипотеза*: регистрация
   целей (`dmVision`/`RegisterDamageThreat`) кладёт в массив целей любую `EntityAI`, а не
   только живых (`IsAlive`/`Man`/`ZombieBase`/`DayZAnimal`). Фикс — фильтр на неживое.

4. `[x]` **Зомби не наносят урон боту, нет порезов (HAK `EEOnDamageCalculated` не работает).**
   *Сделано (v3.45)*: удалён override `EEOnDamageCalculated` (натив `ProcessDirectDamage` не
   реентерабелен → урон не применялся, `return false` отменял ваниль). Теперь ваниль применяет
   полный урон, а в `EEHitBy` для `ZombieBase`-источника возвращается половина шока
   (`AddHealth("","Shock", shock*0.5)`) — Health полный, шок ½. Детали — `docs/research/combat.md`.

5. `[~]` **Прерывистые движения по кругу (доворот) при достижении точки в Follow.**
   *Гипотеза*: доворот корпуса (`ComputeBodyYaw`/`ApplyBodyTurn`/`HeadingModel`) осциллирует
   на финальной точке (расстояние ~0 → постоянный доворот). Связано с F «ступни проскальзывают».

6. `[~]` **Loadout: дробное количество (тряпки 3.3).** *Гипотеза*: спавн/loadout задаёт
   количество предмета через `float` (или `SetQuantity` с дробным) без округления. Фикс —
   `Math.Round`/int.

7. `[~]` **Точность плохая на дальних (>200 м).** *Гипотеза*: на дальних бот не держит паузу
   между выстрелами (отдача/разброс накапливаются). Фикс-идея из отчёта — увеличить паузу
   между выстрелами при дистанции >200 м. Связано с H (точность/dmAiming).

8. `[~]` **Направление дула неправильное при прицеливании** (целится в лицо, дуло напротив
   плеча). *Гипотеза*: round-trip из H — выстрел по `m_Aiming.GetAimDirection()`, а визуальный
   ствол по сглаженным углам `m_AimSmoothedLR/UD`; между ними доворот корпуса даёт рассинхрон.
   Проверить ещё раз (возможно уже частично закрыто H «Выстрел через round-trip»).

9. `[x]` **Смерть: цикл создания трупа и протухания; при здоровье 0 (холод/голод/скрипт) бот
   не труп, стоит мёртвый.** *Сделано (v3.47, внешний килл)*: `modded DayZPlayerImplement.GetHive()`
   возвращает `null` для AI_SERVER (скипает `CharacterKill`-спам), а `EEKilled` зовёт
   `super.EEKilled(killer)` → ваниль делает труп (`InsertCorpse`) + `SendDeathJuncture` (мёртвое
   состояние). **Осталось**: натуральная смерть (голод/холод/скрипт) идёт через `SetHealth`-стат →
   `IsAlive()=false`, но `EEKilled` не приходит → нужен отдельный хук (диагностика `/bot deadmans`).

10. `[~]` **Нокаут «не настоящий»**: лежит, но нельзя взять оружие/«привести в чувство»,
    поза неправильная. *Подтверждено (research/body.md)*: «настоящий» нокаут = `StartCommand_Unconscious(0)`
    **+** `m_IsUnconscious=true` (net-var) **+** `OnUnconsciousStart()` (выброс оружия, `SetSynchDirty`,
    `EnableVoN(false)`). Модный `UpdateUnconsciousBridge` зовёт `StartCommand_Unconscious(0)` голо —
    без `m_IsUnconscious` и `OnUnconsciousStart()`. Фикс: добавить оба (референс — Expansion `eAIBase`
    даёт ИИ action-manager + переопределяет `OnUnconsciousStart`).

11. `[~]` **Не подбирает оружие/мили/патроны** (SHOULDER/MELEE или не-одежда). *Гипотеза*:
    лутание (`dmLoot`/`dmWishlist`/`TakeToAttachmentSlot`) умеет только одежду/предметы,
    слот-маппинг для оружия/патронов/мили не реализован/неверен. Связано с #13.

12. `[~]` **Двери: отбегает далеко (раз в 5).** *Гипотеза*: таймер/дистанция подхода к двери
    (`dmBotIntent_OpenDoor`/`TryOpenDoorOnPath`) слишком велики. Отчёт предлагает таймер 0.15 с
    или дистанцию 0.75 м.

13. `[~]` **Подбирает стройматериалы (не должен), но не патроны/гранаты.** *Гипотеза*: скоринг
    (`dmWishlist.CalcDesired`/`dmLoot.GetCategory`) неверно классифицирует стройматериалы как
    желаемые, а патроны/гранаты — нет; или фильтр подбора сломан. Связано с #11.

14. `[~]` **Не берёт кастет в руку при драке с зомби.** *Гипотеза*: кастет (`KnuckleDuster`)
    не распознаётся выбором мили (`SelectFirearmForRange`/меlee-intent), т.к. классифицируется
    иначе. Нужен тест с кастетом в инвентаре.

15. `[x]` **Слух не работает на выстрелы** (падающие предметы слышит). *Сделано (v3.48)*:
    введён `dmNoiseType` (OTHER/STEP/SCREAM/BULLETIMPACT/SHOT) + сила выстрела по глушителю
    (`dmBotGunshotNoiseStrength`: без 3000м / самодельный `ImprovisedSuppressor` 150м /
    пистолетный `PistolSuppressor` 75м / автоматный `M4`/`AK` и прочее 100м); попадание пули 15м;
    привлекательность `m_Attractiveness` 0.6 для шума/шагов и 0.9 для пули/выстрела; `OnNoise`
    больше не дропает `source==null` (позиционный шум). Детали — `docs/research/perception.md`.

16. `[~]` **Бот в машине вращается (интент Look).** *Гипотеза*: двигательные/поворотные
    интенты не блокируются в транспортном состоянии; Look вращает корпус. Фикс — блокировать
    движение в машине, разрешить голову на 120°.

17. `[~]` **Флаги `m_MeleeAttackRequest`/`m_FireRequest` остаются после нокаута → удар по
    мёртвой цели после вставания.** *Гипотеза*: `ResetActuation` уже сбрасывает `m_FireRequest`
    (v3.36), но `m_MeleeAttackRequest` не сбрасывается; плюс атака не проверяет жива ли цель.

## J. Вождение (автомобиль, `dmBotIntent_Drive`)

Фича «бот водит автомобиль» (v3.192–v3.198). Сборка/деплой/посадка — PASS; сама езда и
достижение цели — **не доведена** (финальный E2E-прогон не выполнен из-за лимита
`dayz-tester`). Состояние и долг:

- **высокий** — **Езда не доведена до PASS.** Толчок `dBodyApplyImpulseAt` в точке двигателя
  при NEUTRAL (последний фикс) — гипотеза работоспособности (по референсу AutoCarMod + ваниль
  `ActionPushCar`), но финальный прогон не сделан. Проверить `speed > 5 км/ч` + `wait carpos`.
  Если плато ~1.4 км/ч сохранится — импульс по-прежнему гасится (проверить `dBodyActive(ACTIVE)`
  + `DisableSimulation(false)` перед импульсом, research-гипотеза #5, и деление на массу по
  `ActionPushCar:38-48`).
- **средний** — **Переключение передач вперёд по скорости — отложено.** На МКПП у серверного
  ИИ сцепление не замыкается → включённая передача сопротивляется импульсу (см. decisions).
  Варианты: (а) АКПП-машина (нативный привод, как Expansion AI), (б) симуляция индикатора
  передачи (synced-var + клиент), (в) `SetClutchState`-путь. Сейчас `ShiftGear` для МКПП ставит
  только NEUTRAL/REVERSE.
- **средний** — **RPM-симуляция не проверена.** `dm_DriveSimRPM` + `OnSound(RPM)` (модный
  `modded CarScript`) пишутся, но на застрявшей скорости (~1.4 км/ч → `t≈0.03`) неотличимы от
  idle. Проверить аудио/визуально после того, как машина поедет.
- **средний** — **Дорожный navmesh-путь фрагментирован.** `FindRoadPath` (ROADWAY-фильтр)
  усечён (в `driving-road` обрыв ~1010 м до цели) → fallback на пеший путь (TODO-костыль в
  `TickDriving`). Правильно — road-graph по образцу Expansion `eAIRoadNetwork`
  (`ObjectIsRoad` + память-точки), крупная отдельная задача.
- **низкий** — **Руль завязан на `speed >= 0.5`** (`DM_DRIVE_STEER_MIN_SPEED`) — машина должна
  разогнаться, чтобы повернуть к цели; на низкой скорости разворот буксует. Возможно, рулить
  и на 0 км/ч (развернуться к вейпоинту до старта толчка).
- **низкий** — **Реверс не ловит буксование** (`TickStuck` порог `speedAbs < 0.5`), машина на
  плато ~1.4 км/ч не считается застрявшей. Пересмотреть детектор на «нет прогресса по дистанции»,
  а не по скорости.

## Сводка по приоритету

- **высокий**: (пусто — крупные задачи закрыты; следующий крупный блок — лут, см. план).
- **средний**: Phase 3 (JSON в FSM); заглушка `HasPlayerSigns`; `FacePoint`;
  мёртвые пресеты/`LoadFSM`; `dmTarget`; чистка ссылок на despawn; выравнивание ходьбы
  (ступни); пауза между точками патруля; пересоздание интентов состоянием (Stealth);
  переработка зрения (регистр 50 м для зомби/животных + игроки 1 км вместо scan-box);
  pathfinding-доводка (плавание, attachment-navmesh, string-pulling).
- **низкий**: `LookAround`, тест распределения весов, пер-канальный
  арбитраж; recovery с пригибанием/прыжком (далёкое будущее); непрерывная «наименьшая
  дискомфортность» корпуса (до Fight).
- **баги тестов (I) — приоритет по «когда начнёт мешать»**: высокий — #4 (урон от зомби),
  #9/#10 (смерть/нокаут), #11/#13 (лут), #3/#17 (цели/флаги); средний — #1/#2 (опасные зоны),
  #5/#8 (движение/прицел), #7 (точность), #12 (двери), #15 (слух), #16 (машина); низкий — #6
  (дробное количество), #14 (кастет).


---

# Тесты тела (отложенные сценарии) — из botorama/TECHDEBT.md

# Техдолг (отложенные работы)

Выполнено — см. `fstructure.md` → «Симуляция тела» и «План развития „человечивание
бота“ — статус». Ниже только то, что осталось.

## 1. Самопроверяемые тесты тела — отложенные сценарии

Эти тесты требуют надёжного механизма индукции состояния (не только «set» стата),
поэтому отложены. При добавлении — по образцу `dmBotTest.c` (summary в чат +
самопроверка по таймеру + 5 с тишины).

| Сценарий | Причина откладывания | Что нужно при реализации |
|---|---|---|
| `/test bot bleed` (кровотечение → кровь → обморок → смерть) | Нужна режущая рана от чего-то (нет готового источника). Вытекание крови уже работает (тик `BleedingSourcesManagerServer`), нужен только авто-источник раны | Подобрать острый ammo-тип и нанести удар через `DamageSystem.CloseCombatDamage` (или прямо завести источник кровотечения) |
| `/test bot cold` (переохлаждение + дрожь) | Надёжная индукция холода не подтверждена | Проверить, что `GetStatHeatBuffer().Set(-20)` реально каскадит в гипотермию/`Tremor` (иначе — телепорт в холодную зону) |
| `/test bot heat` (перегрев/пыхтение) | То же | `GetStatHeatBuffer().Set(20)` или костёр рядом |
| `/test bot tox` (токсичность/отравление) | Каскад «стат → модификатор → здоровье» не подтверждён | `GetStatToxicity().Set(...)` или газовую зону |
| `/test bot drown` (утопление) | Нужна гарантированная вода под ботом | Спавн/телепорт в воду |
| `/test bot blind` (ослепление) | Эффект — сенсорика (след. фаза); `MDF_BLINDED` может сразу деактивироваться без источника | `ActivateModifier(MDF_BLINDED)` + проверка, что не снялся |

## 2. Реакции мозга на состояние тела (следующая фаза)

Сейчас мозг честно не двигает бессознательное/мёртвое тело, но не «реагирует»
осознанно. Отложено:

- Холод → искать тепло (костёр/укрытие/одежду).
- Голод/жажда → искать еду/воду (вместе с симуляцией голода/жажды — статы уже
  тикают, но мозг не ест/не пьёт).
- Мало стамины → отдых/шаг.
- Боль/урон/бой → драка или бегство.
- **Мили-бой (ближний бой)**: сейчас мили-логика отключена (`dmBotMeleeFightLogic_LightHeavy`
  пропускает `HandleFightLogic`, чтобы не падал VM Exception на null-`hcm`). В будущем
  проработать боевой FSM/интенты + кастомную мили-логику (по образцу Expansion
  `eAIMeleeFightLogic_LightHeavy`), тогда и убрать заглушку.
- Ослепление/токсичность/утопление → сенсорика и реакции.
- Болезни (простуда/грипп/пневмония от холода) — модификаторы есть, но нет ни
  реакции, ни тестов.
- **Разбудить бессознательного бота**: у обычного игрока есть меню «привести в чувство»
  (действие по бессознательному телу), а для бота это не работает — ванильное действие
  рассчитано на игроков. Разобраться позже (кастомное действие/взаимодействие с ботом).
- **Связывание бота**: меню связывания есть, но при начале связывания анимация
  прерывается. Ванильное restrain-действие рассчитано на игроков; для бота нужно
  разобраться (кастомное действие/обработка анимации) позже.

## 3. Проверочные риски честной симуляции (следить при прогонах)

- Анимация головы при гейте `ApplyLookVars` в нокауте (не дёргается ли граф).
- Foot-step turn при сбросе (`dmAI_StopTurn`) — не застревает ли `m_TurnState`.
- Устаревший `m_ActualSpeed`/`m_DesiredSpeed` → рывок после выхода из нокаута.
- Синхронизация взгляда на клиенте при гейте.
- `HeadingModel` в нокауте (читает `GetMovementState`).
- Существующие сценарии (`overload`/`patrol`) — боты теперь реально умирают от
  внешнего урона; тесты могут «глохнуть».
- Спавн в воздухе: кик `MoveTo` на 1 м будит физику (бот падает). Если навмеш не
  найден (спавн сильно над рельефом) — кик не сработает, нужен fallback (снап по
  рейкасту/навмешу в `Spawn`).
